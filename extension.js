const vscode = require('vscode');

function activate(context) {
    console.log('EJS Syntax Highlighting extension is now active!');
}

function deactivate() {}

module.exports = {
    activate,
    deactivate
};